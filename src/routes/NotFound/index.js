import React from "react";

const NotFoundPage = () => {
    return(
        <React.Fragment>
            <h1> Error 404. Page not found. </h1>
        </React.Fragment>
    );
}

export default NotFoundPage;